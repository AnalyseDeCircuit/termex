{
  "name": "termex",
  "version": "0.80.0",
  "description": "Open-source AI-native SSH client — Always-on cloud AI workspace powered by SSH",
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.error('⚠️  Tauri dev disabled as of v0.78.0. Use flutter run -d macos / windows / linux from app/. See docs/iterations/v0.78.0-pc-cutover.md'); process.exit(1)\"",
    "build": "node -e \"console.error('⚠️  Tauri build disabled as of v0.78.0. Use flutter build from app/ or scripts/legacy/build-tauri.sh for a forensic Tauri build.'); process.exit(1)\"",
    "preview": "node -e \"console.error('⚠️  Tauri preview disabled as of v0.78.0.'); process.exit(1)\"",
    "lint": "node -e \"console.error('⚠️  Vue lint disabled as of v0.78.0 (frontend retiring).'); process.exit(1)\"",
    "test": "node -e \"console.error('⚠️  Vue tests disabled as of v0.78.0. Run flutter test from app/ instead.'); process.exit(1)\"",
    "tauri": "node -e \"console.error('⚠️  Tauri CLI disabled as of v0.78.0. See scripts/legacy/build-tauri.sh for emergency rebuild.'); process.exit(1)\"",
    "version:bump": "node scripts/bump-version.mjs"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.0",
    "@tauri-apps/api": "^2.0.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-search": "^0.16.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0",
    "dompurify": "^3.3.3",
    "element-plus": "^2.9.0",
    "marked": "^18.0.0",
    "pinia": "^2.2.0",
    "vue": "^3.5.0",
    "vue-i18n": "^9.14.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/dompurify": "^3.2.0",
    "@types/node": "^22.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.2",
    "vue-tsc": "^2.1.0"
  }
}
