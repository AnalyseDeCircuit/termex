/// Application version constants.
///
/// Keep in sync with `app/pubspec.yaml` via `pnpm version:bump`.
library;

const String kAppVersion = '0.49.0';
const String kAppChannel = 'stable';
const String kAppcastBaseUrl = 'https://termex.app/updates';
