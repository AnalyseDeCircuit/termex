// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `from_core`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `fmt`, `fmt`

/// Lists every hop currently associated with `server_id`, ordered by
/// `position`. Returns an empty vec if no chain has been saved.
Future<List<ChainHopDto>> chainListForServer({required String serverId}) =>
    TermexBridge.instance.api
        .crateApiChainChainListForServer(serverId: serverId);

/// Replaces the entire chain for `server_id` with the supplied hops.
/// Pass an empty vec to clear the chain (equivalent to direct connect).
Future<void> chainSaveForServer(
        {required String serverId, required List<ChainHopInputDto> hops}) =>
    TermexBridge.instance.api
        .crateApiChainChainSaveForServer(serverId: serverId, hops: hops);

/// Deletes every hop for `server_id`. Server itself is untouched.
Future<void> chainDeleteForServer({required String serverId}) =>
    TermexBridge.instance.api
        .crateApiChainChainDeleteForServer(serverId: serverId);

/// Snapshot of a single chain hop. The DTO mirrors the storage struct
/// but uses primitive types so it codegen's cleanly through FRB.
class ChainHopDto {
  final String id;
  final String serverId;
  final int position;

  /// `"ssh"` for an SSH bastion hop, `"proxy"` for a network proxy hop.
  final String hopType;

  /// References `servers.id` (bastion) or `proxies.id` (proxy).
  final String hopId;

  /// `"pre"` = before target, `"post"` = after target. Mobile v0.79.20
  /// only writes `pre` hops.
  final String phase;
  final String createdAt;

  const ChainHopDto({
    required this.id,
    required this.serverId,
    required this.position,
    required this.hopType,
    required this.hopId,
    required this.phase,
    required this.createdAt,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      serverId.hashCode ^
      position.hashCode ^
      hopType.hashCode ^
      hopId.hashCode ^
      phase.hashCode ^
      createdAt.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ChainHopDto &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          serverId == other.serverId &&
          position == other.position &&
          hopType == other.hopType &&
          hopId == other.hopId &&
          phase == other.phase &&
          createdAt == other.createdAt;
}

/// Input for replacing the chain of a server. Position is implicit from
/// vec order — the storage layer assigns 0, 1, 2, … in sequence.
class ChainHopInputDto {
  final String hopType;
  final String hopId;
  final String phase;

  const ChainHopInputDto({
    required this.hopType,
    required this.hopId,
    required this.phase,
  });

  @override
  int get hashCode => hopType.hashCode ^ hopId.hashCode ^ phase.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ChainHopInputDto &&
          runtimeType == other.runtimeType &&
          hopType == other.hopType &&
          hopId == other.hopId &&
          phase == other.phase;
}
