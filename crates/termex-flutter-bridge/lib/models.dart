/// Public entry point for the bridge's model types.
///
/// Counterpart to `api.dart` — kept separate so importers can keep the two
/// under distinct prefixes, which is how they are used.
library;

export 'src/models.dart';
