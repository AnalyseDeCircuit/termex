# v0.31.0 — AI Operations Assistant Evolution: AI 运维助手进化

> 状态：**计划中**
> 依赖：v0.30.0、v0.28.0 (monitoring)、v0.29.0 (recording)

---

## 背景

### 当前 AI 能力评估

Termex 的 AI 目前处于 **"工具级"** 能力阶段：

| 功能 | 状态 | 模块 |
|------|------|------|
| 危险命令检测 (40+ 正则规则) | 已完成 | `src-tauri/src/ai/danger.rs` |
| 命令解释 (流式输出) | 已完成 | `ai_explain_command` |
| NL2Cmd (自然语言 -> 命令) | 已完成 | `ai_nl2cmd` |
| 终端内联智能补全 (ghost text) | 已完成 | `useTerminalAutocomplete.ts` |
| 12 种 Provider 支持 | 已完成 | `src-tauri/src/ai/provider.rs` |
| 本地 AI 引擎 (llama-server) | 已完成 | `src-tauri/src/local_ai/` |
| **上下文感知对话** | **未实现** | AI 不知道终端当前状态 |
| **错误自动诊断** | **未实现** | 命令失败后无自动分析 |
| **多轮排障对话** | **未实现** | 无完整对话 UI |
| **命令编排 (Playbook)** | **未实现** | 无多步骤执行引擎 |
| **异常检测联动** | **未实现** | 监控数据未接入 AI |
| **会话总结报告** | **未实现** | 无操作回顾功能 |

### 核心痛点

1. **AI 是"无头"的** — 不知道用户在哪台服务器、什么目录、刚执行了什么命令
2. **错误处理靠人** — `Permission denied` 或 `disk full` 只能用户自己排查
3. **对话不连续** — 每次交互是独立的，无法追问"上一个错误怎么修？"
4. **复杂操作无引导** — "配置 nginx 反向代理"需要用户自己拆解成多个命令

### 目标

将 AI 从"工具级"升级到"助手级" — context-aware、proactive、multi-turn，深度集成 SSH 终端。

---

## 一、Terminal Context Extraction: 终端上下文提取

### 1.1 上下文数据模型

**新建文件**: `src/types/aiContext.ts`

```typescript
/** Terminal context automatically collected for AI conversations. */
export interface TerminalContext {
  /** Server metadata (never includes credentials). */
  server: {
    hostname: string;
    os: string;             // "Ubuntu 22.04", "CentOS 7", etc.
    username: string;
    connectionChain: string; // "local -> bastion -> target"
  };
  /** Current shell state. */
  shell: {
    cwd: string;            // Current working directory
    lastCommand: string;    // Most recently executed command
    lastExitCode: number | null;  // Exit code of last command (null = unknown)
    terminalMode: TerminalMode;   // From useCommandTracker
  };
  /** Recent terminal output (last N lines). */
  recentOutput: string;     // Last 50 lines of terminal buffer (configurable)
  /** Timestamp when context was captured. */
  capturedAt: string;
}

/** Context injection options for AI requests. */
export interface ContextOptions {
  /** Include recent terminal output (default: true). */
  includeOutput: boolean;
  /** Number of recent lines to include (default: 50). */
  outputLines: number;
  /** Include server metadata (default: true). */
  includeServer: boolean;
  /** Include shell state (default: true). */
  includeShell: boolean;
}

/** Default context options. */
export const DEFAULT_CONTEXT_OPTIONS: ContextOptions = {
  includeOutput: true,
  outputLines: 50,
  includeServer: true,
  includeShell: true,
};
```

### 1.2 Context Extraction Composable

**新建文件**: `src/composables/useAiContext.ts` (~180 行)

从 xterm.js buffer 提取终端上下文，复用 `useCommandTracker` 的状态。

```typescript
import type { Terminal } from "@xterm/xterm";
import type { TerminalContext, ContextOptions, DEFAULT_CONTEXT_OPTIONS } from "@/types/aiContext";
import type { CommandLineState, TerminalMode } from "@/types/commandTracker";
import { useSessionStore } from "@/stores/sessionStore";
import { useServerStore } from "@/stores/serverStore";

export function useAiContext(
  getTerminal: () => Terminal | null,
  sessionId: Ref<string>,
  commandState: Ref<CommandLineState>,
  terminalMode: Ref<TerminalMode>,
) {
  /** Extracts current terminal context for AI injection. */
  function captureContext(options: ContextOptions = DEFAULT_CONTEXT_OPTIONS): TerminalContext | null {
    const terminal = getTerminal();
    if (!terminal) return null;

    const sessionStore = useSessionStore();
    const serverStore = useServerStore();
    const session = sessionStore.sessions.get(sessionId.value);
    if (!session) return null;

    const server = serverStore.servers.find(s => s.id === session.serverId);

    // Extract recent output from terminal buffer
    const recentOutput = options.includeOutput
      ? extractRecentLines(terminal, options.outputLines)
      : "";

    return {
      server: options.includeServer ? {
        hostname: server?.host ?? "unknown",
        os: server?.os ?? "unknown",
        username: server?.username ?? "unknown",
        connectionChain: buildChainDescription(session),
      } : { hostname: "", os: "", username: "", connectionChain: "" },
      shell: options.includeShell ? {
        cwd: extractCwd(terminal),
        lastCommand: commandState.value.command,
        lastExitCode: null,  // Populated by error detection
        terminalMode: terminalMode.value,
      } : { cwd: "", lastCommand: "", lastExitCode: null, terminalMode: "unknown" },
      recentOutput,
      capturedAt: new Date().toISOString(),
    };
  }

  return { captureContext };
}

/** Reads last N lines from xterm.js active buffer. */
function extractRecentLines(terminal: Terminal, count: number): string {
  const buffer = terminal.buffer.active;
  const lines: string[] = [];
  const start = Math.max(0, buffer.cursorY + buffer.baseY - count);
  const end = buffer.cursorY + buffer.baseY;

  for (let i = start; i <= end; i++) {
    const line = buffer.getLine(i);
    if (line) {
      lines.push(line.translateToString(true));
    }
  }
  return lines.join("\n");
}
```

### 1.3 Context → System Prompt 构建

**新建文件**: `src-tauri/src/ai/context.rs` (~120 行)

将前端传入的 context 格式化为 system prompt 注入 AI 请求。

```rust
use serde::Deserialize;

/// Terminal context sent from frontend for AI conversations.
#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TerminalContext {
    pub server: ServerContext,
    pub shell: ShellContext,
    pub recent_output: String,
    pub captured_at: String,
}

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ServerContext {
    pub hostname: String,
    pub os: String,
    pub username: String,
    pub connection_chain: String,
}

#[derive(Debug, Clone, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ShellContext {
    pub cwd: String,
    pub last_command: String,
    pub last_exit_code: Option<i32>,
    pub terminal_mode: String,
}

/// Builds a system prompt with terminal context injected.
pub fn build_context_prompt(base_prompt: &str, ctx: &TerminalContext) -> String {
    let mut prompt = String::with_capacity(base_prompt.len() + 512);
    prompt.push_str(base_prompt);
    prompt.push_str("\n\n--- Terminal Context ---\n");

    if !ctx.server.hostname.is_empty() {
        prompt.push_str(&format!(
            "Server: {}@{} ({})\nConnection: {}\n",
            ctx.server.username, ctx.server.hostname,
            ctx.server.os, ctx.server.connection_chain,
        ));
    }

    if !ctx.shell.cwd.is_empty() {
        prompt.push_str(&format!("CWD: {}\n", ctx.shell.cwd));
    }
    if !ctx.shell.last_command.is_empty() {
        prompt.push_str(&format!("Last command: {}\n", ctx.shell.last_command));
    }
    if let Some(code) = ctx.shell.last_exit_code {
        prompt.push_str(&format!("Exit code: {}\n", code));
    }

    if !ctx.recent_output.is_empty() {
        prompt.push_str(&format!(
            "\n--- Recent Terminal Output ---\n{}\n--- End Output ---\n",
            ctx.recent_output
        ));
    }

    prompt
}
```

**安全规则**: `TerminalContext` 不包含任何凭据字段。Server password / API key / private key 绝不会被注入到 AI prompt。`ServerContext` 仅包含 hostname、OS、username — 与 v0.5.0 的安全规则一致。

---

## 二、Multi-Turn Chat: 多轮对话系统

### 2.1 Chat 数据模型扩展

**修改文件**: `src/types/ai.ts`

```typescript
/** Extended message types for multi-turn chat. */
export type ChatRole = "user" | "assistant" | "system" | "context";

export interface ChatMessage {
  id: string;
  role: ChatRole;
  content: string;
  timestamp: string;
  /** For assistant messages: whether AI is still streaming. */
  streaming?: boolean;
  /** For context messages: auto-injected terminal context. */
  context?: TerminalContext;
  /** For assistant messages: extracted executable commands. */
  commands?: ExtractedCommand[];
}

/** A command extracted from AI response (for one-click insert). */
export interface ExtractedCommand {
  /** The command text. */
  command: string;
  /** AI's explanation of what this command does. */
  description: string;
  /** Whether this command was flagged as potentially dangerous. */
  dangerous: boolean;
}

/** Per-session chat conversation. */
export interface ChatConversation {
  sessionId: string;
  messages: ChatMessage[];
  /** System prompt customization (optional). */
  systemPrompt?: string;
  createdAt: string;
}
```

### 2.2 Chat IPC Command

**修改文件**: `src-tauri/src/commands/ai.rs`

新增 `ai_chat` 命令 — 支持多轮对话 + 上下文注入 + 流式响应。

```rust
/// Input for multi-turn AI chat.
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChatInput {
    pub session_id: String,
    pub message: String,
    pub include_context: bool,
    pub context: Option<crate::ai::context::TerminalContext>,
    /// Previous messages for multi-turn context (last N turns).
    pub history: Vec<ChatHistoryEntry>,
}

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChatHistoryEntry {
    pub role: String,     // "user" | "assistant"
    pub content: String,
}

/// Multi-turn AI chat with optional terminal context injection.
#[tauri::command]
pub async fn ai_chat(
    app: AppHandle,
    state: State<'_, AppState>,
    input: ChatInput,
    request_id: String,
) -> Result<(), String> {
    let provider_config = get_default_provider(&state)?;
    let provider = create_provider(&provider_config)?;

    // Build system prompt with context
    let base_prompt = "You are Termex AI, an expert Linux/Unix operations assistant. \
        You help users diagnose issues, write commands, and manage servers. \
        When suggesting commands, wrap each in a ```bash code block. \
        Be concise and actionable.";

    let system_prompt = if input.include_context {
        if let Some(ref ctx) = input.context {
            crate::ai::context::build_context_prompt(base_prompt, ctx)
        } else {
            base_prompt.to_string()
        }
    } else {
        base_prompt.to_string()
    };

    // Build conversation messages
    let mut messages = Vec::new();
    for entry in &input.history {
        messages.push(format!("{}: {}", entry.role, entry.content));
    }
    messages.push(format!("user: {}", input.message));
    let full_message = messages.join("\n\n");

    let request = AiRequest {
        system_prompt,
        user_message: full_message,
        max_tokens: provider_config.max_tokens as u32,
    };

    let event_name = format!("ai://chat/{}", request_id);
    let app_clone = app.clone();
    provider
        .stream(
            &request,
            Box::new(move |chunk| {
                let _ = app_clone.emit(&event_name, &chunk);
            }),
        )
        .await
        .map_err(|e| e.to_string())
}
```

### 2.3 Chat Store 扩展

**修改文件**: `src/stores/aiStore.ts`

```typescript
// 新增 state
const conversations = ref<Map<string, ChatConversation>>(new Map());
const activeChatSessionId = ref<string | null>(null);

/** Gets or creates a conversation for a session. */
function getConversation(sessionId: string): ChatConversation {
  let conv = conversations.value.get(sessionId);
  if (!conv) {
    conv = {
      sessionId,
      messages: [],
      createdAt: new Date().toISOString(),
    };
    conversations.value.set(sessionId, conv);
  }
  return conv;
}

/** Sends a chat message with optional terminal context. */
async function sendChat(
  sessionId: string,
  message: string,
  context: TerminalContext | null,
): Promise<void> {
  const conv = getConversation(sessionId);
  const requestId = crypto.randomUUID();

  // Add user message
  conv.messages.push({
    id: crypto.randomUUID(),
    role: "user",
    content: message,
    timestamp: new Date().toISOString(),
  });

  // Add context message (collapsed in UI)
  if (context) {
    conv.messages.push({
      id: crypto.randomUUID(),
      role: "context",
      content: "Terminal context attached",
      timestamp: new Date().toISOString(),
      context,
    });
  }

  // Add streaming assistant message placeholder
  const assistantMsg: ChatMessage = {
    id: requestId,
    role: "assistant",
    content: "",
    timestamp: new Date().toISOString(),
    streaming: true,
    commands: [],
  };
  conv.messages.push(assistantMsg);

  // Build history (last 10 turns for API context)
  const history = conv.messages
    .filter(m => m.role === "user" || m.role === "assistant")
    .slice(-10)
    .map(m => ({ role: m.role, content: m.content }));

  // Listen for streaming chunks
  const unlisten = await tauriListen<AiChunk>(
    `ai://chat/${requestId}`,
    (chunk) => {
      const target = conv.messages.find(m => m.id === requestId);
      if (target) {
        target.content += chunk.text;
        if (chunk.done) {
          target.streaming = false;
          target.commands = extractCommands(target.content);
        }
      }
    },
  );

  try {
    await tauriInvoke("ai_chat", {
      input: {
        sessionId,
        message,
        includeContext: !!context,
        context,
        history: history.slice(0, -1),  // Exclude current assistant placeholder
      },
      requestId,
    });
  } catch (err) {
    assistantMsg.content = String(err);
    assistantMsg.streaming = false;
  }

  unlisten();
}

/** Extracts bash commands from AI markdown response. */
function extractCommands(content: string): ExtractedCommand[] {
  const commands: ExtractedCommand[] = [];
  const regex = /```(?:bash|sh|shell)?\n([\s\S]*?)```/g;
  let match: RegExpExecArray | null;
  while ((match = regex.exec(content)) !== null) {
    const cmd = match[1].trim();
    if (cmd) {
      commands.push({
        command: cmd,
        description: "",
        dangerous: false,
      });
    }
  }
  return commands;
}

/** Clears conversation for a session (on disconnect or manual clear). */
function clearConversation(sessionId: string): void {
  conversations.value.delete(sessionId);
}
```

### 2.4 Chat UI 重构

**重构文件**: `src/components/ai/AiPanel.vue`

从简单消息列表升级为完整的对话界面。

```
+-----------------------------------------------+
| AI Assistant                    [Settings] [X] |
+-----------------------------------------------+
| [Context: ON]  [Auto-diagnose: ON]             |
+-----------------------------------------------+
|                                                 |
| [system] Connected to web-prod (Ubuntu 22.04)  |
|                                                 |
| [user] Why is the server so slow?              |
|                                                 |
| [context] Terminal context attached  [expand]   |
|                                                 |
| [assistant]                                     |
| Based on your terminal output, I can see high  |
| CPU usage. Here are diagnostic steps:          |
|                                                 |
| ```bash                                         |
| top -bn1 -o %CPU | head -20                    |
| ```                                 [Insert ->] |
|                                                 |
| ```bash                                         |
| journalctl --since "1 hour ago" -p err         |
| ```                                 [Insert ->] |
|                                                 |
+-----------------------------------------------+
| [Include context]  Type message...      [Send] |
+-----------------------------------------------+
```

**新建文件**: `src/components/ai/AiChatMessage.vue` (~150 行)

单条消息组件，支持 4 种角色渲染：

| 角色 | 样式 | 交互 |
|------|------|------|
| `user` | 右对齐蓝色气泡 | 无 |
| `assistant` | 左对齐灰色气泡 | 代码块 → "Insert" 按钮、"Copy" 按钮 |
| `context` | 居中灰色小字 | 可展开查看完整上下文 |
| `system` | 居中淡色标签 | 无 |

```typescript
// AiChatMessage.vue - key props
const props = defineProps<{
  message: ChatMessage;
}>();
const emit = defineEmits<{
  (e: "insert-command", command: string): void;
}>();

// Parse markdown code blocks for "Insert" button rendering
const parsedBlocks = computed(() => parseMessageContent(props.message.content));
```

**新建文件**: `src/components/ai/CommandSequence.vue` (~120 行)

用于 AI 命令编排的步骤列表组件 (详见第四节)。

**新建文件**: `src/components/ai/ErrorDiagnosis.vue` (~100 行)

用于错误自动诊断的通知 + 分析面板 (详见第三节)。

---

## 三、Error Auto-Diagnosis: 错误自动诊断

### 3.1 Error Detection

**新建文件**: `src/composables/useErrorDetection.ts` (~200 行)

在终端输出中检测命令执行错误，支持两种检测策略。

```typescript
/** Error detection result. */
export interface DetectedError {
  /** The command that produced the error. */
  command: string;
  /** Error output lines from terminal. */
  errorOutput: string;
  /** Matched error pattern (for debugging). */
  matchedPattern: string;
  /** Severity: info (退出码非零), warning (known error), critical (data loss risk). */
  severity: "info" | "warning" | "critical";
  /** Timestamp of detection. */
  detectedAt: string;
}

/** Error patterns categorized by severity. */
const ERROR_PATTERNS: Array<{ pattern: RegExp; severity: DetectedError["severity"]; label: string }> = [
  // Critical (data loss / security risk)
  { pattern: /No space left on device/i, severity: "critical", label: "disk_full" },
  { pattern: /Read-only file system/i, severity: "critical", label: "readonly_fs" },
  { pattern: /Segmentation fault/i, severity: "critical", label: "segfault" },
  { pattern: /kernel panic/i, severity: "critical", label: "kernel_panic" },
  { pattern: /Out of memory/i, severity: "critical", label: "oom" },
  { pattern: /FATAL/i, severity: "critical", label: "fatal" },

  // Warning (common operational errors)
  { pattern: /command not found/i, severity: "warning", label: "cmd_not_found" },
  { pattern: /Permission denied/i, severity: "warning", label: "permission_denied" },
  { pattern: /No such file or directory/i, severity: "warning", label: "no_such_file" },
  { pattern: /Connection refused/i, severity: "warning", label: "conn_refused" },
  { pattern: /Connection timed out/i, severity: "warning", label: "conn_timeout" },
  { pattern: /Name or service not known/i, severity: "warning", label: "dns_fail" },
  { pattern: /Could not resolve hostname/i, severity: "warning", label: "dns_fail" },
  { pattern: /Authentication failure/i, severity: "warning", label: "auth_fail" },
  { pattern: /error:\s/i, severity: "warning", label: "generic_error" },
  { pattern: /E:\s+Unable to/i, severity: "warning", label: "apt_error" },
  { pattern: /Failed to start/i, severity: "warning", label: "service_fail" },
  { pattern: /Unit .+ not found/i, severity: "warning", label: "unit_not_found" },
  { pattern: /nginx: \[emerg\]/i, severity: "warning", label: "nginx_error" },
  { pattern: /ERROR \d{4}/i, severity: "warning", label: "mysql_error" },
  { pattern: /ENOSPC/i, severity: "warning", label: "disk_full_node" },
  { pattern: /EACCES/i, severity: "warning", label: "permission_node" },
  { pattern: /Cannot allocate memory/i, severity: "warning", label: "memory" },
  { pattern: /Too many open files/i, severity: "warning", label: "fd_limit" },
  { pattern: /Address already in use/i, severity: "warning", label: "port_in_use" },

  // Info (non-zero exit code patterns)
  { pattern: /exit code [1-9]\d*/i, severity: "info", label: "nonzero_exit" },
  { pattern: /returned [1-9]\d*/i, severity: "info", label: "nonzero_return" },
  { pattern: /failed\b/i, severity: "info", label: "generic_failed" },
];

export function useErrorDetection(
  getTerminal: () => Terminal | null,
  sessionId: Ref<string>,
  commandState: Ref<CommandLineState>,
) {
  const lastError = ref<DetectedError | null>(null);
  const autodiagnoseEnabled = ref(true);
  let debounceTimer: ReturnType<typeof setTimeout> | null = null;

  /** Called after terminal output stabilizes (debounced). */
  function checkForErrors(): void {
    const terminal = getTerminal();
    if (!terminal || !commandState.value.atPrompt) return;

    // Read last 20 lines (command output region)
    const output = extractRecentLines(terminal, 20);
    if (!output) return;

    for (const { pattern, severity, label } of ERROR_PATTERNS) {
      if (pattern.test(output)) {
        lastError.value = {
          command: commandState.value.command,
          errorOutput: output,
          matchedPattern: label,
          severity,
          detectedAt: new Date().toISOString(),
        };
        break;  // First match wins (patterns ordered by severity)
      }
    }
  }

  // Hook into terminal.onWriteParsed with 500ms debounce
  // ...
}
```

### 3.2 Auto-Diagnosis Flow

```
Terminal output  -->  useErrorDetection  -->  error detected?
                                                  |
                                                  v yes
                                          autodiagnoseEnabled?
                                                  |
                                                  v yes
                                          captureContext() + error info
                                                  |
                                                  v
                                          ai_diagnose (backend)
                                                  |
                                                  v
                                          toast notification:
                                          "AI detected an error. [View]"
                                                  |
                                                  v (user clicks)
                                          ErrorDiagnosis panel opens
                                          in AI sidebar
```

### 3.3 Diagnosis Backend Command

**修改文件**: `src-tauri/src/commands/ai.rs`

```rust
/// Input for automatic error diagnosis.
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct DiagnoseInput {
    pub session_id: String,
    pub command: String,
    pub error_output: String,
    pub context: Option<crate::ai::context::TerminalContext>,
}

/// Diagnose a command error using AI.
#[tauri::command]
pub async fn ai_diagnose(
    app: AppHandle,
    state: State<'_, AppState>,
    input: DiagnoseInput,
    request_id: String,
) -> Result<(), String> {
    let provider_config = get_default_provider(&state)?;
    let provider = create_provider(&provider_config)?;

    let base_prompt = "You are an expert Linux systems administrator. \
        A command failed on a remote server. Analyze the error and provide: \
        1. Root cause (one sentence) \
        2. Fix command(s) wrapped in ```bash blocks \
        3. Prevention tip (one sentence). \
        Be concise.";

    let system_prompt = if let Some(ref ctx) = input.context {
        crate::ai::context::build_context_prompt(base_prompt, ctx)
    } else {
        base_prompt.to_string()
    };

    let user_msg = format!(
        "Command: `{}`\n\nError output:\n```\n{}\n```",
        input.command, input.error_output
    );

    let request = AiRequest {
        system_prompt,
        user_message: user_msg,
        max_tokens: 1024,
    };

    let event_name = format!("ai://diagnose/{}", request_id);
    let app_clone = app.clone();
    provider
        .stream(
            &request,
            Box::new(move |chunk| {
                let _ = app_clone.emit(&event_name, &chunk);
            }),
        )
        .await
        .map_err(|e| e.to_string())
}
```

### 3.4 Diagnosis UI

**新建文件**: `src/components/ai/ErrorDiagnosis.vue`

```
+-----------------------------------------------+
| ! Error Detected                        [Dismiss] |
+-----------------------------------------------+
| Command:  apt install nginx                     |
| Error:    E: Unable to locate package nginx     |
+-----------------------------------------------+
| AI Analysis:                                    |
|                                                 |
| Root cause: Package list outdated.              |
|                                                 |
| Fix:                                            |
| ```bash                                         |
| sudo apt update && sudo apt install nginx       |
| ```                               [Run Fix ->]  |
|                                                 |
| Tip: Run `apt update` before installing.        |
+-----------------------------------------------+
```

### 3.5 Settings

**修改文件**: `src/types/settings.ts` — 新增 AI 诊断相关设置

```typescript
export interface AiDiagnosisSettings {
  /** Enable auto-diagnosis (default: true). */
  enabled: boolean;
  /** Minimum severity to trigger diagnosis (default: "warning"). */
  minSeverity: "info" | "warning" | "critical";
  /** Show toast notification (default: true). */
  showToast: boolean;
  /** Auto-expand AI panel on error (default: false). */
  autoExpandPanel: boolean;
}
```

---

## 四、AI Command Orchestration: 命令编排引擎

### 4.1 设计理念

用户描述一个高级目标（如"部署 Node.js 应用到 nginx"），AI 将其分解为一系列有序命令步骤。用户可以逐步执行（点击每个步骤）或一键全部运行。每步执行后 AI 检查输出判断是否成功。

类似 Ansible Playbook，但完全交互式、即时生成。

### 4.2 Orchestration 数据模型

**修改文件**: `src/types/ai.ts`

```typescript
/** A step in an AI-generated command sequence. */
export interface OrchestratedStep {
  id: string;
  /** Step number (1-based). */
  stepNumber: number;
  /** Human-readable description. */
  description: string;
  /** The command to execute. */
  command: string;
  /** Step state. */
  status: "pending" | "running" | "success" | "failed" | "skipped";
  /** Terminal output after execution (for AI validation). */
  output?: string;
  /** AI validation result after execution. */
  validation?: string;
  /** Whether the command is dangerous (requires extra confirmation). */
  dangerous: boolean;
}

/** A complete orchestrated command sequence (Playbook). */
export interface Playbook {
  id: string;
  /** The user's original goal description. */
  goal: string;
  /** Generated steps. */
  steps: OrchestratedStep[];
  /** Overall status. */
  status: "generating" | "ready" | "executing" | "completed" | "failed";
  createdAt: string;
}
```

### 4.3 Orchestration Backend

**新建文件**: `src-tauri/src/ai/orchestrator.rs` (~150 行)

```rust
use serde::{Deserialize, Serialize};
use crate::ai::provider::{AiProvider, AiRequest, AiError};

/// A parsed step from AI orchestration response.
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ParsedStep {
    pub step_number: u32,
    pub description: String,
    pub command: String,
    pub dangerous: bool,
}

/// Parses AI response into structured command steps.
///
/// Expected AI output format:
/// ```
/// STEP 1: Install nginx
/// ```bash
/// sudo apt install -y nginx
/// ```
///
/// STEP 2: Create config file
/// ```bash
/// sudo tee /etc/nginx/sites-available/myapp > /dev/null <<'EOF'
/// server { ... }
/// EOF
/// ```
/// ```
pub fn parse_orchestration_response(content: &str) -> Vec<ParsedStep> {
    let mut steps = Vec::new();
    let step_re = regex::Regex::new(
        r"(?i)STEP\s+(\d+)[:\s]+(.+?)(?:\r?\n)```(?:bash|sh|shell)?\n([\s\S]*?)```"
    ).unwrap();

    for cap in step_re.captures_iter(content) {
        let step_num: u32 = cap[1].parse().unwrap_or(0);
        let desc = cap[2].trim().to_string();
        let cmd = cap[3].trim().to_string();

        // Check if command is dangerous (reuse DangerDetector)
        let danger = crate::ai::danger::DangerDetector::check(&cmd);

        steps.push(ParsedStep {
            step_number: step_num,
            description: desc,
            command: cmd,
            dangerous: danger.is_dangerous,
        });
    }
    steps
}

/// System prompt for orchestration requests.
pub const ORCHESTRATION_PROMPT: &str = "\
You are Termex AI, an expert operations assistant. \
The user will describe a goal. Generate a step-by-step command sequence. \
\n\nFormat EXACTLY as:\n\
STEP 1: <description>\n```bash\n<command>\n```\n\n\
STEP 2: <description>\n```bash\n<command>\n```\n\n\
Rules:\n\
- One command per step (pipe chains are OK)\n\
- Include error checking where appropriate (e.g., `set -e`)\n\
- Use sudo only when necessary\n\
- Prefer non-destructive commands\n\
- Maximum 15 steps";
```

### 4.4 Orchestration IPC

**修改文件**: `src-tauri/src/commands/ai.rs`

```rust
/// Orchestrate a command sequence for a complex goal.
#[tauri::command]
pub async fn ai_orchestrate(
    app: AppHandle,
    state: State<'_, AppState>,
    goal: String,
    context: Option<crate::ai::context::TerminalContext>,
    request_id: String,
) -> Result<(), String> {
    let provider_config = get_default_provider(&state)?;
    let provider = create_provider(&provider_config)?;

    let system_prompt = if let Some(ref ctx) = context {
        crate::ai::context::build_context_prompt(
            crate::ai::orchestrator::ORCHESTRATION_PROMPT, ctx
        )
    } else {
        crate::ai::orchestrator::ORCHESTRATION_PROMPT.to_string()
    };

    let request = AiRequest {
        system_prompt,
        user_message: goal,
        max_tokens: 4096,
    };

    let event_name = format!("ai://orchestrate/{}", request_id);
    let app_clone = app.clone();
    provider
        .stream(
            &request,
            Box::new(move |chunk| {
                let _ = app_clone.emit(&event_name, &chunk);
            }),
        )
        .await
        .map_err(|e| e.to_string())
}
```

### 4.5 Step Execution UI

**新建文件**: `src/components/ai/CommandSequence.vue`

```
+-----------------------------------------------+
| Playbook: Deploy Node.js app with nginx        |
| Status: Ready (5 steps)          [Run All] [X] |
+-----------------------------------------------+
| [v] 1. Update package list                     |
|     sudo apt update                  [success] |
+-----------------------------------------------+
| [ ] 2. Install nginx                           |
|     sudo apt install -y nginx         [Run ->] |
+-----------------------------------------------+
| [ ] 3. Create nginx config                     |
|     sudo tee /etc/nginx/sites-...    [pending] |
+-----------------------------------------------+
| [ ] 4. Enable site                             |
|     sudo ln -s /etc/nginx/sites-...  [pending] |
+-----------------------------------------------+
| [!] 5. Restart nginx            [dangerous]    |
|     sudo systemctl restart nginx [Confirm+Run] |
+-----------------------------------------------+
```

执行流程:
1. 用户点击 "Run" 或 "Run All"
2. 将 command 通过 `ssh_write` 发送到终端
3. 等待命令执行完成 (检测到 prompt 返回)
4. 读取终端输出 → 发送给 AI 验证
5. AI 返回 `success` / `failed` + 原因
6. 成功 → 继续下一步；失败 → 暂停并展示 AI 建议

---

## 五、Anomaly Detection Integration: 异常检测联动

### 5.1 与 v0.28.0 监控数据集成

当 v0.28.0 的监控系统检测到阈值突破 (CPU > 90%, memory > 90%, disk > 95%) 时，触发 AI 分析。

**修改文件**: `src/composables/useAiContext.ts` — 新增监控数据注入

```typescript
/** Monitor alert for AI analysis. */
export interface MonitorAlert {
  metric: "cpu" | "memory" | "disk" | "load";
  value: number;
  threshold: number;
  /** Top processes contributing to the metric. */
  topProcesses?: string[];
  timestamp: string;
}
```

**事件流**:

```
v0.28.0 monitor event (ssh://monitor/{session_id})
    |
    v
useAiContext.onMonitorAlert(alert)
    |
    v
ai_diagnose({
  command: "[automatic] monitor alert",
  error_output: "CPU at 95%. Top: mysql (45%), java (30%)",
  context: terminalContext,
})
    |
    v
Status bar notification:
"CPU spike detected. AI: mysql slow query likely. [View Analysis]"
```

### 5.2 Status Bar 集成

**修改文件**: `src/components/terminal/StatusBar.vue`

在现有 status bar 中新增 AI 诊断指示器：

```
[user@host:~/app] [UTF-8] [LF] [AI: 1 alert] [80x24]
```

点击 `[AI: 1 alert]` 展开 AI 面板显示最新诊断。

---

## 六、Session Summary Report: 会话总结报告

### 6.1 Summary Generation

**修改文件**: `src-tauri/src/commands/ai.rs`

```rust
/// Generate a session summary report.
#[tauri::command]
pub async fn ai_session_summary(
    app: AppHandle,
    state: State<'_, AppState>,
    terminal_buffer: String,
    context: Option<crate::ai::context::TerminalContext>,
    request_id: String,
) -> Result<(), String> {
    let provider_config = get_default_provider(&state)?;
    let provider = create_provider(&provider_config)?;

    let system_prompt = "You are Termex AI. Summarize a terminal session. Provide:\n\
        1. **Operations performed** - bullet list of what the user did\n\
        2. **Configuration changes** - any files modified, services restarted\n\
        3. **Errors encountered** - errors and how they were resolved\n\
        4. **Recommendations** - any follow-up actions needed\n\
        Format as markdown.";

    let request = AiRequest {
        system_prompt: system_prompt.to_string(),
        user_message: format!("Terminal session buffer:\n```\n{}\n```", terminal_buffer),
        max_tokens: 2048,
    };

    let event_name = format!("ai://summary/{}", request_id);
    let app_clone = app.clone();
    provider
        .stream(
            &request,
            Box::new(move |chunk| {
                let _ = app_clone.emit(&event_name, &chunk);
            }),
        )
        .await
        .map_err(|e| e.to_string())
}
```

### 6.2 前端 Summary 按钮

在 AiPanel header 中新增 "Summarize" 按钮。点击后读取当前终端的完整 buffer 内容 (最多 500 行)，发送到后端生成报告。报告以 `assistant` 消息形式显示在对话中，并提供 "Export as Markdown" 按钮。

---

## 七、新增/修改文件清单

### 新增文件

| 文件 | 类型 | 行数估计 | 说明 |
|------|------|---------|------|
| `src/types/aiContext.ts` | TypeScript | ~60 | Terminal context + options 类型定义 |
| `src/composables/useAiContext.ts` | TypeScript | ~180 | 终端上下文提取 composable |
| `src/composables/useErrorDetection.ts` | TypeScript | ~200 | 错误模式检测 + 自动诊断触发 |
| `src/components/ai/AiChatMessage.vue` | Vue | ~150 | 单条聊天消息组件 (多角色渲染) |
| `src/components/ai/CommandSequence.vue` | Vue | ~120 | Playbook 步骤列表 UI |
| `src/components/ai/ErrorDiagnosis.vue` | Vue | ~100 | 错误诊断通知 + 分析面板 |
| `src-tauri/src/ai/context.rs` | Rust | ~120 | Context prompt 构建 |
| `src-tauri/src/ai/orchestrator.rs` | Rust | ~150 | 命令编排响应解析 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `src-tauri/src/ai/mod.rs` | 导出 `context`, `orchestrator` 模块 |
| `src-tauri/src/commands/ai.rs` | 新增 `ai_chat`, `ai_diagnose`, `ai_orchestrate`, `ai_session_summary` 4 个命令 |
| `src-tauri/src/commands/mod.rs` | 注册新命令到 Tauri handler |
| `src/types/ai.ts` | 新增 `ChatMessage`, `ExtractedCommand`, `ChatConversation`, `OrchestratedStep`, `Playbook` 类型 |
| `src/stores/aiStore.ts` | 新增 `conversations`, `sendChat`, `clearConversation`, `extractCommands` |
| `src/components/ai/AiPanel.vue` | 重构为多轮对话 UI + summarize 按钮 |
| `src/components/ai/AiMessage.vue` | 复用 → 部分逻辑迁入 AiChatMessage |
| `src/components/terminal/TerminalView.vue` | 集成 `useAiContext` + `useErrorDetection` |
| `src/components/terminal/StatusBar.vue` | 新增 AI 诊断指示器 |

---

## 八、安全合规

### 不发送到 AI 的信息 (CRITICAL)

- SSH 密码 / 私钥 / passphrase
- AI Provider API Key
- 数据库 master password
- Keychain 内容
- `.env` 文件内容 (如终端 `cat .env` 的输出出现在 buffer 中 — 当前版本不做过滤，v0.33 考虑)

### 发送到 AI 的信息

- 服务器 hostname、OS、username (已有先例: v0.5.0 的 explain/nl2cmd)
- 终端 buffer 文本 (用户可见的内容)
- 用户主动输入的消息
- 命令文本

### 用户控制

- 每次 chat 可选择是否附带 context (`include_context: bool`)
- Auto-diagnose 可全局关闭
- AI panel 关闭时不监听终端输出
- 清除对话历史按钮

---

## 九、测试计划

### 9.1 Rust 单元测试

**文件**: `src-tauri/tests/test_ai_context.rs`

| 测试 | 验证内容 |
|------|---------|
| `test_build_context_prompt_full` | 完整 context 注入到 system prompt |
| `test_build_context_prompt_empty_fields` | 空字段不产生占位文本 |
| `test_build_context_prompt_no_output` | `recent_output` 为空时不注入 output 块 |
| `test_parse_orchestration_single_step` | 单步骤解析 |
| `test_parse_orchestration_multi_step` | 5 步骤解析 + 正确序号 |
| `test_parse_orchestration_dangerous_step` | 包含 `rm -rf` 的步骤标记为 dangerous |
| `test_parse_orchestration_malformed` | 格式不规范的 AI 输出容错 |

### 9.2 TypeScript 单元测试

**文件**: `src/__tests__/useErrorDetection.test.ts`

| 测试 | 验证内容 |
|------|---------|
| `detects "command not found" as warning` | 基本模式匹配 |
| `detects "No space left" as critical` | 高危模式 |
| `ignores normal output` | `ls` 输出不触发检测 |
| `severity priority is correct` | 多模式匹配时返回最高严重级 |
| `debounce prevents duplicate alerts` | 500ms 内重复输出不重复报警 |

**文件**: `src/__tests__/extractCommands.test.ts`

| 测试 | 验证内容 |
|------|---------|
| `extracts single bash block` | 单个代码块 |
| `extracts multiple blocks` | 多个代码块 |
| `handles block without language tag` | ` ```\ncommand\n``` ` |
| `ignores non-bash blocks` | ` ```json ` 不提取 |
| `handles empty blocks` | 空代码块不生成 command |

### 9.3 手动集成测试清单

- [ ] 连接服务器 → 打开 AI panel → 发送 "where am I?" → AI 包含 hostname + CWD
- [ ] 执行 `cat nonexistent` → toast 出现 → 点击查看 AI 分析
- [ ] AI panel 发送 "install nginx step by step" → 生成 Playbook → 逐步执行
- [ ] 关闭 auto-diagnose → 执行错误命令 → 不出现 toast
- [ ] "Summarize this session" → 生成 markdown 报告 → 导出成功
- [ ] 多轮对话: 追问 "how to fix this?" → AI 引用之前的上下文
- [ ] 断开连接 → 对话历史清除
- [ ] 无 AI Provider 配置 → 所有功能优雅降级 (显示配置引导)

---

## 补充设计 A：AI Provider 流式调用方案

### A.1 现状分析

当前 AI 调用是**一次性返回**模式：`call_ai_provider()` 发起 HTTP 请求，等待完整响应后通过 Tauri 事件发送到前端。`AiProvider` trait 虽定义了 `stream()` 方法但未实现。

### A.2 改造方案：真流式输出

**修改文件**: `src-tauri/src/ai/provider.rs`

为每个 provider 实现真正的 SSE/streaming 调用，使用 `reqwest` 的 `bytes_stream()` 逐 chunk 解析：

```rust
/// Streaming callback type.
pub type StreamCallback = Box<dyn Fn(AiChunk) + Send + 'static>;

/// Streaming AI chunk.
#[derive(Debug, Clone, Serialize)]
pub struct AiChunk {
    pub text: String,
    pub done: bool,
}

/// 统一流式调用入口（各 provider 按自己的 SSE 格式解析）
pub async fn stream_ai_provider(
    state: &AppState,
    provider_type: &str,
    api_key: &str,
    base_url: Option<&str>,
    model: &str,
    system: &str,
    user_msg: &str,
    max_tokens: Option<u32>,
    on_chunk: StreamCallback,
) -> Result<(), String> {
    match provider_type {
        "openai" | "deepseek" | "grok" | "mistral" | "glm" | "minimax" | "doubao" | "custom" => {
            stream_openai_compatible(api_key, base_url, model, system, user_msg, max_tokens, on_chunk).await
        }
        "claude" => {
            stream_claude(api_key, base_url, model, system, user_msg, max_tokens, on_chunk).await
        }
        "gemini" => {
            stream_gemini(api_key, base_url, model, system, user_msg, max_tokens, on_chunk).await
        }
        "ollama" | "local" => {
            stream_ollama(base_url, model, system, user_msg, max_tokens, on_chunk).await
        }
        _ => Err(format!("unsupported provider: {provider_type}")),
    }
}
```

**OpenAI-compatible SSE 流式解析**:

```rust
async fn stream_openai_compatible(
    api_key: &str,
    base_url: Option<&str>,
    model: &str,
    system: &str,
    user_msg: &str,
    max_tokens: Option<u32>,
    on_chunk: StreamCallback,
) -> Result<(), String> {
    let url = format!("{}/chat/completions", base_url.unwrap_or("https://api.openai.com/v1"));

    let body = serde_json::json!({
        "model": model,
        "messages": [
            { "role": "system", "content": system },
            { "role": "user", "content": user_msg },
        ],
        "max_tokens": max_tokens.unwrap_or(2048),
        "stream": true,  // 关键：启用 SSE 流
    });

    let resp = reqwest::Client::new()
        .post(&url)
        .header("Authorization", format!("Bearer {api_key}"))
        .header("Content-Type", "application/json")
        .json(&body)
        .send()
        .await
        .map_err(|e| e.to_string())?;

    let mut stream = resp.bytes_stream();
    let mut buffer = String::new();

    while let Some(chunk) = futures_util::StreamExt::next(&mut stream).await {
        let bytes = chunk.map_err(|e| e.to_string())?;
        buffer.push_str(&String::from_utf8_lossy(&bytes));

        // SSE: lines prefixed with "data: "
        while let Some(pos) = buffer.find('\n') {
            let line = buffer[..pos].to_string();
            buffer = buffer[pos + 1..].to_string();

            if let Some(data) = line.strip_prefix("data: ") {
                if data.trim() == "[DONE]" {
                    on_chunk(AiChunk { text: String::new(), done: true });
                    return Ok(());
                }
                if let Ok(json) = serde_json::from_str::<serde_json::Value>(data) {
                    if let Some(delta) = json["choices"][0]["delta"]["content"].as_str() {
                        on_chunk(AiChunk { text: delta.to_string(), done: false });
                    }
                }
            }
        }
    }

    on_chunk(AiChunk { text: String::new(), done: true });
    Ok(())
}
```

### A.3 新命令调用模式

`ai_chat` / `ai_diagnose` / `ai_orchestrate` 命令改为调用 `stream_ai_provider`，每个 chunk 即时 emit 到前端：

```rust
#[tauri::command]
pub async fn ai_chat(
    app: AppHandle,
    state: State<'_, AppState>,
    input: ChatInput,
    request_id: String,
) -> Result<(), String> {
    let (provider_type, api_key, base_url, model) = get_default_provider_info(&state)?;
    let system_prompt = build_system_prompt(&input);

    let event_name = format!("ai://chat/{request_id}");
    let app_clone = app.clone();

    stream_ai_provider(
        state.inner(),
        &provider_type,
        &api_key,
        base_url.as_deref(),
        &model,
        &system_prompt,
        &input.message,
        Some(4096),
        Box::new(move |chunk| {
            let _ = app_clone.emit(&event_name, &chunk);
        }),
    ).await
}
```

### A.4 不改造的 Provider（降级为一次性）

如果某个 provider 不支持 SSE（如某些自定义 API），降级为一次性调用：

```rust
async fn stream_fallback(/* ... */, on_chunk: StreamCallback) -> Result<(), String> {
    // 一次性获取完整响应
    let full_response = call_ai_provider(/* ... */).await?;
    // 模拟流式：分段发送
    for line in full_response.lines() {
        on_chunk(AiChunk { text: format!("{line}\n"), done: false });
    }
    on_chunk(AiChunk { text: String::new(), done: true });
    Ok(())
}
```

---

## 补充设计 B：Server.os 字段获取方案

### B.1 问题

`ServerContext.os` 引用了 `server?.os`，但 Server struct 无 os 字段。

### B.2 方案：从 Monitor OsInfo 获取

不新增 Server 字段。改为从 `monitorStore` 的 `osInfo` 获取（v0.28.0 已实现）：

```typescript
// useAiContext.ts
import { useMonitorStore } from "@/stores/monitorStore";

function captureContext(/* ... */): TerminalContext | null {
  const monitorStore = useMonitorStore();
  const osInfo = monitorStore.getOsInfo(sessionId.value);

  return {
    server: {
      hostname: server?.host ?? "unknown",
      os: osInfo?.prettyName ?? osInfo?.name ?? "unknown",
      username: server?.username ?? "unknown",
      connectionChain: buildChainDescription(session),
    },
    // ...
  };
}
```

如果 monitor 未启动（osInfo 为 null），fallback 为 `"unknown"`。这不影响 AI 质量——AI 会从终端输出中推断 OS。

---

## 补充设计 C：Markdown 渲染方案

### C.1 技术选型

使用 **`marked`** 库（轻量、0 依赖、16KB gzip）+ **DOMPurify** 防 XSS：

```bash
pnpm add marked dompurify
pnpm add -D @types/dompurify
```

### C.2 渲染 composable

**新建文件**: `src/composables/useMarkdown.ts` (~40 行)

```typescript
import { marked } from "marked";
import DOMPurify from "dompurify";

// Configure marked
marked.setOptions({
  breaks: true,
  gfm: true,
});

/** Renders markdown to sanitized HTML. */
export function renderMarkdown(markdown: string): string {
  const raw = marked.parse(markdown, { async: false }) as string;
  return DOMPurify.sanitize(raw, {
    ALLOWED_TAGS: ["p", "br", "strong", "em", "code", "pre", "ul", "ol", "li",
                    "h1", "h2", "h3", "blockquote", "a", "span"],
    ALLOWED_ATTR: ["class", "href", "target"],
  });
}
```

### C.3 代码块特殊处理

`AiChatMessage.vue` 需要将 AI 回复中的 ` ```bash ` 代码块替换为可交互组件（含 Insert / Copy 按钮），而非纯 HTML `<pre><code>`。

方案：先分割内容为 text blocks + code blocks，分别渲染：

```typescript
interface ContentBlock {
  type: "text" | "code";
  content: string;
  language?: string;
}

function parseBlocks(content: string): ContentBlock[] {
  const blocks: ContentBlock[] = [];
  const regex = /```(\w*)\n([\s\S]*?)```/g;
  let lastIndex = 0;

  let match: RegExpExecArray | null;
  while ((match = regex.exec(content)) !== null) {
    // Text before code block
    if (match.index > lastIndex) {
      blocks.push({ type: "text", content: content.slice(lastIndex, match.index) });
    }
    // Code block
    blocks.push({ type: "code", content: match[2].trim(), language: match[1] || "bash" });
    lastIndex = match.index + match[0].length;
  }
  // Remaining text
  if (lastIndex < content.length) {
    blocks.push({ type: "text", content: content.slice(lastIndex) });
  }
  return blocks;
}
```

模板中：
```vue
<template v-for="(block, i) in parsedBlocks" :key="i">
  <!-- Text block: render markdown -->
  <div v-if="block.type === 'text'" class="prose" v-html="renderMarkdown(block.content)" />
  <!-- Code block: interactive -->
  <div v-else class="code-block">
    <pre class="text-xs font-mono p-2 rounded" style="background: #1e293b"><code>{{ block.content }}</code></pre>
    <div class="code-actions">
      <button @click="emit('insert-command', block.content)">Insert</button>
      <button @click="copyToClipboard(block.content)">Copy</button>
    </div>
  </div>
</template>
```

---

## 补充设计 D：AiPanel.vue 重构方案

### D.1 向后兼容策略

当前 AiPanel 是简单的 message list + input。重构为多轮对话 UI 时：

1. **保留 `messages` ref** — 现有 `explainCommand` 产生的消息继续以旧格式显示
2. **新增 `conversations` map** — 按 sessionId 隔离对话
3. **UI 自动切换**：当 `activeChatSessionId` 存在时显示多轮对话模式，否则显示旧模式

### D.2 完整重构代码

**修改文件**: `src/components/ai/AiPanel.vue`

```vue
<script setup lang="ts">
import { ref, computed, watch, nextTick } from "vue";
import { useI18n } from "vue-i18n";
import { useAiStore } from "@/stores/aiStore";
import { useSessionStore } from "@/stores/sessionStore";
import AiChatMessage from "./AiChatMessage.vue";

const { t } = useI18n();
const aiStore = useAiStore();
const sessionStore = useSessionStore();
const emit = defineEmits<{
  (e: "insert-command", cmd: string): void;
  (e: "open-settings"): void;
}>();

const inputText = ref("");
const scrollRef = ref<HTMLElement>();
const includeContext = ref(true);

// Active session's conversation
const activeSessionId = computed(() => sessionStore.activeSessionId);
const conversation = computed(() => {
  if (!activeSessionId.value) return null;
  return aiStore.getConversation(activeSessionId.value);
});

// Auto-scroll on new message
watch(
  () => conversation.value?.messages.length,
  async () => {
    await nextTick();
    if (scrollRef.value) {
      scrollRef.value.scrollTop = scrollRef.value.scrollHeight;
    }
  },
);

async function handleSend() {
  const text = inputText.value.trim();
  if (!text || !activeSessionId.value) return;
  inputText.value = "";

  // Capture terminal context if enabled
  // (context captured from TerminalView via event)
  const context = includeContext.value
    ? window.__termexCaptureContext?.(activeSessionId.value) ?? null
    : null;

  await aiStore.sendChat(activeSessionId.value, text, context);
}

function handleClear() {
  if (activeSessionId.value) {
    aiStore.clearConversation(activeSessionId.value);
  }
}
</script>

<template>
  <div class="flex flex-col h-full" style="background: var(--tm-bg-surface)">
    <!-- Header -->
    <div class="flex items-center h-9 px-3 shrink-0" style="border-bottom: 1px solid var(--tm-border)">
      <span class="text-xs font-semibold flex-1" style="color: var(--tm-text-primary)">
        {{ t("ai.panelTitle") }}
      </span>
      <button class="tm-icon-btn p-1 rounded" @click="handleClear">
        <span class="text-xs">{{ t("ai.clear") }}</span>
      </button>
    </div>

    <!-- No provider state -->
    <div v-if="aiStore.providers.length === 0" class="flex-1 flex items-center justify-center">
      <div class="text-center text-xs p-4" style="color: var(--tm-text-muted)">
        <p>{{ t("ai.noProviderHint") }}</p>
        <el-button size="small" class="mt-2" @click="emit('open-settings')">
          {{ t("ai.goConfig") }}
        </el-button>
      </div>
    </div>

    <!-- Chat area -->
    <template v-else>
      <div ref="scrollRef" class="flex-1 overflow-y-auto px-3 py-2 space-y-3">
        <AiChatMessage
          v-for="msg in conversation?.messages ?? []"
          :key="msg.id"
          :message="msg"
          @insert-command="emit('insert-command', $event)"
        />
      </div>

      <!-- Input area -->
      <div class="shrink-0 px-3 py-2" style="border-top: 1px solid var(--tm-border)">
        <div class="flex items-center gap-1 mb-1">
          <el-checkbox v-model="includeContext" size="small">
            {{ t("ai.includeContext") }}
          </el-checkbox>
        </div>
        <div class="flex gap-2">
          <el-input
            v-model="inputText"
            type="textarea"
            :rows="2"
            :placeholder="t('ai.inputPlaceholder')"
            @keydown.enter.ctrl="handleSend"
          />
          <el-button
            type="primary"
            size="small"
            :disabled="!inputText.trim()"
            @click="handleSend"
          >
            {{ t("ai.send") }}
          </el-button>
        </div>
      </div>
    </template>
  </div>
</template>
```

### D.3 Context 采集桥接

TerminalView 通过全局函数注册 context 采集器：

```typescript
// TerminalView.vue onMounted 中
window.__termexCaptureContext = (sessionId: string) => {
  if (props.sessionId !== sessionId) return null;
  return aiContext.captureContext();
};

// 类型声明 (src/env.d.ts)
declare global {
  interface Window {
    __termexCaptureContext?: (sessionId: string) => TerminalContext | null;
  }
}
```

---

## 补充设计 E：Playbook 执行引擎

### E.1 命令完成检测

复用 `useCommandTracker` 的 prompt 检测机制。当 `commandState.atPrompt` 从 `false` 变回 `true` 时，认为命令执行完毕。

```typescript
// usePlaybookExecutor.ts
export function usePlaybookExecutor(
  sessionId: Ref<string>,
  commandState: Ref<CommandLineState>,
  getTerminal: () => Terminal | null,
) {
  const activePlaybook = ref<Playbook | null>(null);
  const currentStepIndex = ref(-1);

  /** Executes the next pending step. */
  async function executeStep(stepIndex: number): Promise<void> {
    const playbook = activePlaybook.value;
    if (!playbook) return;
    const step = playbook.steps[stepIndex];
    if (!step || step.status !== "pending") return;

    step.status = "running";
    currentStepIndex.value = stepIndex;

    // Send command to terminal
    const bytes = new TextEncoder().encode(step.command + "\n");
    const writeCmd = sessionId.value.startsWith("local-") ? "local_pty_write" : "ssh_write";
    await tauriInvoke(writeCmd, { sessionId: sessionId.value, data: Array.from(bytes) });

    // Wait for prompt to return (command completion)
    await waitForPrompt(commandState, 30_000); // 30s timeout

    // Capture output
    const terminal = getTerminal();
    if (terminal) {
      step.output = extractRecentLines(terminal, 30);
    }

    // AI validation
    step.validation = await validateStep(step);
    step.status = step.validation?.includes("SUCCESS") ? "success" : "failed";
  }

  /** Runs all pending steps sequentially. */
  async function runAll(): Promise<void> {
    const playbook = activePlaybook.value;
    if (!playbook) return;
    playbook.status = "executing";

    for (let i = 0; i < playbook.steps.length; i++) {
      if (playbook.steps[i].status !== "pending") continue;
      if (playbook.steps[i].dangerous) {
        // Pause for user confirmation
        playbook.status = "ready";
        return;
      }
      await executeStep(i);
      if (playbook.steps[i].status === "failed") {
        playbook.status = "failed";
        return;
      }
    }
    playbook.status = "completed";
  }

  return { activePlaybook, currentStepIndex, executeStep, runAll };
}

/** Waits until the terminal returns to a prompt. */
function waitForPrompt(commandState: Ref<CommandLineState>, timeoutMs: number): Promise<void> {
  return new Promise((resolve, reject) => {
    const timeout = setTimeout(() => reject("timeout"), timeoutMs);
    const stop = watch(
      () => commandState.value.atPrompt,
      (atPrompt) => {
        if (atPrompt) {
          clearTimeout(timeout);
          stop();
          // Small delay to ensure output is fully written
          setTimeout(resolve, 200);
        }
      },
    );
  });
}
```

### E.2 步骤验证 AI 调用

**修改文件**: `src-tauri/src/commands/ai.rs`

```rust
/// Validates a playbook step execution result.
#[tauri::command]
pub async fn ai_validate_step(
    state: State<'_, AppState>,
    step_description: String,
    step_command: String,
    step_output: String,
) -> Result<String, String> {
    let system = "You are validating a command execution. \
        Given the step description, command, and output, respond with exactly one word: \
        SUCCESS if the command achieved its goal, or FAILED followed by a brief reason.";

    let user_msg = format!(
        "Step: {}\nCommand: {}\nOutput:\n```\n{}\n```",
        step_description, step_command, step_output,
    );

    call_ai_provider(state.inner(), /* ... */, system, &user_msg, Some(256)).await
}
```

---

## 补充设计 F：异常检测联动完整方案

### F.1 阈值配置

复用 settingsStore，新增 AI 诊断阈值设置：

```typescript
// settingsStore.ts 新增
const aiAlertCpuThreshold = ref(90);
const aiAlertMemoryThreshold = ref(90);
const aiAlertDiskThreshold = ref(95);
const aiAlertEnabled = ref(true);
```

### F.2 监控事件触发

**修改文件**: `src/composables/useMonitor.ts`

在现有 `healthStatus` 计算中增加 AI 告警触发：

```typescript
// 当 metrics 更新时检查阈值
watch(() => monitorStore.getLatest(sessionId), (metrics) => {
  if (!metrics || !settingsStore.aiAlertEnabled) return;

  if (metrics.cpu.usagePercent > settingsStore.aiAlertCpuThreshold) {
    emitMonitorAlert({
      metric: "cpu",
      value: metrics.cpu.usagePercent,
      threshold: settingsStore.aiAlertCpuThreshold,
      topProcesses: metrics.processes?.slice(0, 5).map(p => `${p.name} (${p.cpu}%)`),
      timestamp: new Date().toISOString(),
    });
  }
  // Same for memory, disk...
}, { deep: true });

function emitMonitorAlert(alert: MonitorAlert) {
  // Debounce: only alert once per metric per 5 minutes
  window.dispatchEvent(new CustomEvent("termex:monitor-alert", { detail: { sessionId, alert } }));
}
```

### F.3 useAiContext 监听

```typescript
// useAiContext.ts 新增
function onMonitorAlert(sessionId: string, alert: MonitorAlert) {
  if (!autodiagnoseEnabled.value) return;

  const context = captureContext();
  const errorOutput = `${alert.metric.toUpperCase()} at ${alert.value}% (threshold: ${alert.threshold}%)\n` +
    (alert.topProcesses ? `Top processes: ${alert.topProcesses.join(", ")}` : "");

  aiStore.sendChat(sessionId, `[Auto] Monitor alert: ${errorOutput}`, context);
}
```

---

## 补充设计 G：AiChatMessage.vue 完整代码

**新建文件**: `src/components/ai/AiChatMessage.vue` (~150 行)

```vue
<script setup lang="ts">
import { computed } from "vue";
import { useI18n } from "vue-i18n";
import { renderMarkdown } from "@/composables/useMarkdown";
import type { ChatMessage } from "@/types/ai";

const { t } = useI18n();
const props = defineProps<{ message: ChatMessage }>();
const emit = defineEmits<{
  (e: "insert-command", cmd: string): void;
}>();

interface ContentBlock {
  type: "text" | "code";
  content: string;
  language?: string;
}

const parsedBlocks = computed<ContentBlock[]>(() => {
  if (props.message.role !== "assistant") return [];
  const blocks: ContentBlock[] = [];
  const regex = /```(\w*)\n([\s\S]*?)```/g;
  let lastIndex = 0;
  let match: RegExpExecArray | null;
  const content = props.message.content;

  while ((match = regex.exec(content)) !== null) {
    if (match.index > lastIndex) {
      blocks.push({ type: "text", content: content.slice(lastIndex, match.index) });
    }
    blocks.push({ type: "code", content: match[2].trim(), language: match[1] || "bash" });
    lastIndex = match.index + match[0].length;
  }
  if (lastIndex < content.length) {
    blocks.push({ type: "text", content: content.slice(lastIndex) });
  }
  return blocks;
});

const contextExpanded = ref(false);

function copyText(text: string) {
  navigator.clipboard.writeText(text);
}
</script>

<template>
  <!-- User message -->
  <div v-if="message.role === 'user'" class="flex justify-end">
    <div class="max-w-[85%] px-3 py-1.5 rounded-lg text-xs"
         style="background: var(--el-color-primary); color: white">
      {{ message.content }}
    </div>
  </div>

  <!-- Context marker -->
  <div v-else-if="message.role === 'context'" class="text-center">
    <button
      class="text-[10px] px-2 py-0.5 rounded transition-colors"
      style="color: var(--tm-text-muted); background: var(--tm-bg-hover)"
      @click="contextExpanded = !contextExpanded"
    >
      {{ message.content }} {{ contextExpanded ? "▼" : "▶" }}
    </button>
    <pre v-if="contextExpanded" class="text-[10px] text-left mt-1 p-2 rounded overflow-auto"
         style="background: var(--tm-bg-hover); color: var(--tm-text-muted); max-height: 200px">
{{ JSON.stringify(message.context, null, 2) }}
    </pre>
  </div>

  <!-- System message -->
  <div v-else-if="message.role === 'system'" class="text-center">
    <span class="text-[10px] px-2 py-0.5 rounded"
          style="color: var(--tm-text-muted); background: var(--tm-bg-hover)">
      {{ message.content }}
    </span>
  </div>

  <!-- Assistant message -->
  <div v-else class="max-w-[95%]">
    <!-- Streaming indicator -->
    <div v-if="message.streaming" class="text-xs animate-pulse mb-1"
         style="color: var(--tm-text-muted)">
      {{ t("ai.thinking") }}
    </div>

    <!-- Parsed content blocks -->
    <template v-if="parsedBlocks.length > 0">
      <template v-for="(block, i) in parsedBlocks" :key="i">
        <!-- Text block: markdown rendered -->
        <div v-if="block.type === 'text'"
             class="text-xs leading-relaxed prose-sm"
             style="color: var(--tm-text-primary)"
             v-html="renderMarkdown(block.content)" />
        <!-- Code block: interactive -->
        <div v-else class="my-1.5 rounded overflow-hidden" style="border: 1px solid var(--tm-border)">
          <pre class="text-[11px] font-mono p-2 m-0 overflow-x-auto"
               style="background: #0d1117; color: #e6edf3"><code>{{ block.content }}</code></pre>
          <div class="flex justify-end gap-1 px-2 py-1" style="background: var(--tm-bg-hover)">
            <button class="text-[10px] px-2 py-0.5 rounded transition-colors hover:bg-white/10"
                    style="color: var(--el-color-primary)"
                    @click="emit('insert-command', block.content)">
              {{ t("ai.insert") }}
            </button>
            <button class="text-[10px] px-2 py-0.5 rounded transition-colors hover:bg-white/10"
                    style="color: var(--tm-text-muted)"
                    @click="copyText(block.content)">
              {{ t("ai.copy") }}
            </button>
          </div>
        </div>
      </template>
    </template>

    <!-- Fallback: plain text (non-assistant or no code blocks) -->
    <div v-else class="text-xs whitespace-pre-wrap"
         style="color: var(--tm-text-primary)">
      {{ message.content }}
    </div>
  </div>
</template>
```

---

## 补充设计 H：CommandSequence.vue 完整代码

**新建文件**: `src/components/ai/CommandSequence.vue` (~120 行)

```vue
<script setup lang="ts">
import { useI18n } from "vue-i18n";
import type { Playbook, OrchestratedStep } from "@/types/ai";

const { t } = useI18n();

const props = defineProps<{
  playbook: Playbook;
}>();
const emit = defineEmits<{
  (e: "run-step", index: number): void;
  (e: "run-all"): void;
  (e: "close"): void;
}>();

function statusIcon(status: OrchestratedStep["status"]): string {
  switch (status) {
    case "success": return "✓";
    case "failed": return "✗";
    case "running": return "⟳";
    case "skipped": return "–";
    default: return "○";
  }
}

function statusColor(status: OrchestratedStep["status"]): string {
  switch (status) {
    case "success": return "#22c55e";
    case "failed": return "#ef4444";
    case "running": return "#3b82f6";
    default: return "var(--tm-text-muted)";
  }
}
</script>

<template>
  <div class="rounded overflow-hidden" style="border: 1px solid var(--tm-border)">
    <!-- Header -->
    <div class="flex items-center gap-2 px-3 py-2" style="background: var(--tm-bg-hover)">
      <span class="text-xs font-medium flex-1" style="color: var(--tm-text-primary)">
        {{ playbook.goal }}
      </span>
      <el-button
        v-if="playbook.status === 'ready'"
        size="small"
        type="primary"
        @click="emit('run-all')"
      >
        {{ t("ai.runAll") }}
      </el-button>
      <button class="text-xs" style="color: var(--tm-text-muted)" @click="emit('close')">✕</button>
    </div>

    <!-- Steps -->
    <div v-for="(step, i) in playbook.steps" :key="step.id"
         class="flex items-start gap-2 px-3 py-2 text-xs"
         style="border-top: 1px solid var(--tm-border)">
      <!-- Status icon -->
      <span class="shrink-0 w-4 text-center font-mono" :style="{ color: statusColor(step.status) }">
        {{ statusIcon(step.status) }}
      </span>

      <!-- Step info -->
      <div class="flex-1 min-w-0">
        <div style="color: var(--tm-text-secondary)">
          {{ step.stepNumber }}. {{ step.description }}
        </div>
        <pre class="text-[11px] font-mono mt-1 p-1.5 rounded overflow-x-auto"
             style="background: #0d1117; color: #e6edf3; margin: 0">{{ step.command }}</pre>
        <div v-if="step.validation" class="text-[10px] mt-1" style="color: var(--tm-text-muted)">
          {{ step.validation }}
        </div>
      </div>

      <!-- Run button -->
      <el-button
        v-if="step.status === 'pending'"
        size="small"
        :type="step.dangerous ? 'danger' : 'primary'"
        text
        @click="emit('run-step', i)"
      >
        {{ step.dangerous ? t("ai.confirmRun") : t("ai.run") }}
      </el-button>
      <span v-else-if="step.status === 'running'" class="animate-spin text-blue-400">⟳</span>
    </div>
  </div>
</template>
```

---

## 补充设计 I：ErrorDiagnosis.vue 完整代码

**新建文件**: `src/components/ai/ErrorDiagnosis.vue` (~100 行)

```vue
<script setup lang="ts">
import { ref } from "vue";
import { useI18n } from "vue-i18n";
import { renderMarkdown } from "@/composables/useMarkdown";
import type { DetectedError } from "@/composables/useErrorDetection";

const { t } = useI18n();

const props = defineProps<{
  error: DetectedError;
  diagnosis: string;
  loading: boolean;
}>();
const emit = defineEmits<{
  (e: "insert-command", cmd: string): void;
  (e: "dismiss"): void;
}>();

// Extract fix commands from diagnosis
const fixCommands = computed(() => {
  const regex = /```(?:bash|sh)?\n([\s\S]*?)```/g;
  const cmds: string[] = [];
  let match: RegExpExecArray | null;
  while ((match = regex.exec(props.diagnosis)) !== null) {
    cmds.push(match[1].trim());
  }
  return cmds;
});
</script>

<template>
  <div class="rounded overflow-hidden" style="border: 1px solid var(--tm-border)">
    <!-- Header -->
    <div class="flex items-center gap-2 px-3 py-2"
         :style="{
           background: error.severity === 'critical' ? 'rgba(239,68,68,0.1)' : 'rgba(234,179,8,0.1)',
           borderBottom: '1px solid var(--tm-border)',
         }">
      <span class="text-xs font-medium"
            :style="{ color: error.severity === 'critical' ? '#ef4444' : '#eab308' }">
        {{ t("ai.errorDetected") }}
      </span>
      <div class="flex-1" />
      <button class="text-xs" style="color: var(--tm-text-muted)" @click="emit('dismiss')">
        {{ t("ai.dismiss") }}
      </button>
    </div>

    <!-- Error info -->
    <div class="px-3 py-2 space-y-1">
      <div class="text-[11px]" style="color: var(--tm-text-secondary)">
        <strong>{{ t("ai.command") }}:</strong> <code>{{ error.command }}</code>
      </div>
      <pre class="text-[10px] font-mono p-1.5 rounded overflow-x-auto max-h-20"
           style="background: var(--tm-bg-hover); color: var(--tm-text-muted); margin: 0">{{ error.errorOutput.slice(0, 500) }}</pre>
    </div>

    <!-- AI Analysis -->
    <div class="px-3 py-2" style="border-top: 1px solid var(--tm-border)">
      <div v-if="loading" class="text-xs animate-pulse" style="color: var(--tm-text-muted)">
        {{ t("ai.analyzing") }}
      </div>
      <div v-else class="text-xs" style="color: var(--tm-text-primary)" v-html="renderMarkdown(diagnosis)" />

      <!-- Quick fix buttons -->
      <div v-if="fixCommands.length > 0" class="flex flex-wrap gap-1 mt-2">
        <el-button
          v-for="(cmd, i) in fixCommands"
          :key="i"
          size="small"
          type="primary"
          text
          @click="emit('insert-command', cmd)"
        >
          {{ t("ai.runFix") }}: {{ cmd.slice(0, 40) }}{{ cmd.length > 40 ? "..." : "" }}
        </el-button>
      </div>
    </div>
  </div>
</template>
```

---

## 补充设计 J：useErrorDetection 集成方案

### J.1 TerminalView.vue 集成

```typescript
// TerminalView.vue 中
import { useErrorDetection } from "@/composables/useErrorDetection";

const errorDetection = useErrorDetection(
  getTerminal,
  sessionIdRef,
  commandTracker.state,
);

// 在 mount 后初始化
onMounted(async () => {
  // ... existing mount logic ...
  errorDetection.init();
});
```

### J.2 init() 实现

```typescript
function init() {
  const terminal = getTerminal();
  if (!terminal) return;

  // Hook into terminal data — debounced error check
  terminal.onWriteParsed(() => {
    if (debounceTimer) clearTimeout(debounceTimer);
    debounceTimer = setTimeout(checkForErrors, 500);
  });
}
```

### J.3 Error → AI Panel 联动

通过 window 自定义事件传递到 App.vue → AiPanel：

```typescript
// useErrorDetection.ts
watch(lastError, (error) => {
  if (!error || !autodiagnoseEnabled.value) return;
  window.dispatchEvent(new CustomEvent("termex:error-detected", {
    detail: { sessionId: sessionId.value, error },
  }));
});

// App.vue 监听
window.addEventListener("termex:error-detected", (e: CustomEvent) => {
  if (settingsStore.aiDiagnoseAutoExpand) {
    aiPanelVisible.value = true;
  }
  ElNotification({
    title: t("ai.errorDetected"),
    message: e.detail.error.matchedPattern,
    type: "warning",
    onClick: () => { aiPanelVisible.value = true; },
  });
});
```

---

## 补充设计 K：Chat 持久化方案

### K.1 设计决策

**不持久化到 DB** — 对话仅在内存中保留。原因：

1. 对话包含终端输出片段，可能含敏感信息
2. 断开连接后上下文已失效，旧对话价值低
3. 避免 DB 膨胀

### K.2 生命周期

- **创建**: 用户首次在某 session 发送 chat 消息时
- **保留**: 整个 session 生命周期内
- **清除**: session disconnect 时自动清除 / 用户手动点击 "Clear"
- **切换 tab**: 对话随 session 切换，每个 tab 独立

---

## 补充设计 L：i18n 新增键

### L.1 完整键列表

```typescript
// en-US
ai: {
  // ... existing keys ...
  includeContext: "Include terminal context",
  thinking: "Thinking...",
  errorDetected: "Error Detected",
  analyzing: "Analyzing error...",
  dismiss: "Dismiss",
  command: "Command",
  runFix: "Run fix",
  runAll: "Run All",
  run: "Run",
  confirmRun: "Confirm & Run",
  playbook: "Playbook",
  playbookGenerating: "Generating steps...",
  playbookReady: "Ready ({count} steps)",
  stepSuccess: "Success",
  stepFailed: "Failed",
  summarize: "Summarize Session",
  summarizing: "Generating summary...",
  exportMarkdown: "Export as Markdown",
  diagnosisTitle: "AI Diagnosis",
  alertCpuThreshold: "CPU alert threshold (%)",
  alertMemoryThreshold: "Memory alert threshold (%)",
  alertDiskThreshold: "Disk alert threshold (%)",
  autoDiagnose: "Auto-diagnose errors",
  autoDiagnoseHint: "AI automatically analyzes command errors",
},

// zh-CN
ai: {
  // ... existing keys ...
  includeContext: "附带终端上下文",
  thinking: "思考中...",
  errorDetected: "检测到错误",
  analyzing: "正在分析错误...",
  dismiss: "忽略",
  command: "命令",
  runFix: "执行修复",
  runAll: "全部执行",
  run: "执行",
  confirmRun: "确认并执行",
  playbook: "操作手册",
  playbookGenerating: "正在生成步骤...",
  playbookReady: "就绪（{count} 个步骤）",
  stepSuccess: "成功",
  stepFailed: "失败",
  summarize: "会话总结",
  summarizing: "正在生成总结...",
  exportMarkdown: "导出为 Markdown",
  diagnosisTitle: "AI 诊断",
  alertCpuThreshold: "CPU 告警阈值 (%)",
  alertMemoryThreshold: "内存告警阈值 (%)",
  alertDiskThreshold: "磁盘告警阈值 (%)",
  autoDiagnose: "自动诊断错误",
  autoDiagnoseHint: "AI 自动分析命令错误",
},
```

---

## 补充设计 M：Settings 集成

### M.1 settingsStore 新增

```typescript
// settingsStore.ts
const aiDiagnoseEnabled = ref(true);
const aiDiagnoseMinSeverity = ref<"info" | "warning" | "critical">("warning");
const aiDiagnoseAutoExpand = ref(false);
const aiAlertCpuThreshold = ref(90);
const aiAlertMemoryThreshold = ref(90);
const aiAlertDiskThreshold = ref(95);
const aiAlertEnabled = ref(true);
```

### M.2 Settings UI

在现有 AI 配置 tab 中添加"诊断设置"分区：

```vue
<!-- AiConfigTab.vue 底部新增 -->
<el-divider />
<h4 class="text-xs font-medium mb-2" style="color: var(--tm-text-primary)">
  {{ t("ai.diagnosisTitle") }}
</h4>

<div class="flex items-center gap-3 mb-2">
  <label class="text-xs" style="color: var(--tm-text-secondary)">
    {{ t("ai.autoDiagnose") }}
  </label>
  <el-switch v-model="settings.aiDiagnoseEnabled" />
</div>

<div class="flex items-center gap-3 mb-2">
  <label class="text-xs shrink-0" style="color: var(--tm-text-secondary)">
    {{ t("ai.alertCpuThreshold") }}
  </label>
  <el-input-number v-model="settings.aiAlertCpuThreshold" :min="50" :max="100" size="small" />
</div>
<!-- Similar for memory and disk thresholds -->
```

---

## 补充设计 N：lib.rs 命令注册

```rust
// src-tauri/src/lib.rs invoke_handler 新增:
commands::ai::ai_chat,
commands::ai::ai_diagnose,
commands::ai::ai_orchestrate,
commands::ai::ai_validate_step,
commands::ai::ai_session_summary,
```

同时在 `src-tauri/src/ai/mod.rs` 中新增模块声明：

```rust
pub mod context;
pub mod orchestrator;
```

---

## 十、已知限制与后续迭代

1. **Buffer 安全过滤**: 终端 buffer 可能包含敏感信息 (`cat /etc/shadow` 输出)。当前版本原样发送，v0.33 将增加敏感内容过滤层
2. **Exit code 检测**: 依赖 prompt 启发式（v0.26.0 的 `useCommandTracker`），精度约 85%。后续可通过 `trap 'echo $?' DEBUG` 注入提升
3. **Orchestration 验证**: 步骤间 AI 验证需要额外 API 调用（增加延迟和成本）。可选配置
4. **本地 AI 适配**: 小型本地模型 (7B) 的编排质量可能不佳。建议使用 13B+ 或 API 模型
5. **对话持久化**: 当前对话仅存内存，断开即清除。v0.33 可选持久化到 SQLCipher
6. **并发诊断**: 多个 tab 同时出错时共用同一 AI 请求队列，避免 API 过载
